export const DEFAULT_ENVIRONMENTS = ["development", "staging", "production"] as const;

export const ROLES = ["owner", "admin", "member"] as const;

export const TOKEN_PREFIX = "evp_";

export const TOKEN_EXPIRY_DAYS = 90;

export const CONFIG_DIR = ".envpush";

export const CONFIG_FILE = "config.json";

export const PROJECT_CONFIG_FILE = ".evp.json";

export const DEFAULT_PORT = 8787;

export const VERSION = "0.1.0";
